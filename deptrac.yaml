parameters:
  paths:
    - ./src

  layers:
    - name: Legacy
      collectors:
        - type: className
          regex: ^Bavix\\.*\\Objects\\Cart$

    - name: Contract
      collectors:
        - type: className
          regex: ^Bavix\\.*\\Objects\\Cart$

    - name: Contract
      collectors:
        - type: className
